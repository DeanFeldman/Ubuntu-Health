{
  "name": "clinic-system",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "clean:clinics": "node scripts/cleanClinicsOU5.js",
    "seed:clinics": "node scripts/seedClinics.js",
     "test:coverage": "npm run test:coverage --prefix client && npm run test:coverage --prefix server"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
